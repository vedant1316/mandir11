"""
Cricket Scorer Engine — Phase 2 placeholder.

Ball-by-ball scoring over WebSocket will be implemented in Phase 2.
See MANDIR11-REFERENCE.md section 13 — Build Phases.

Phase 2 responsibilities:
  - Accept ball events (runs, wides, wickets) over WebSocket ws/match/{id}
  - Validate legal ball count per over (wides don't count)
  - Manage over/innings rollover
  - Handle wicket + next batter selection
  - Sync full innings state on reconnect
  - Support Test match multi-innings (innings_number 1 & 2)
"""

# No Phase 1 implementation here. Do not add placeholder logic.
